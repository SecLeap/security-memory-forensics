# 恶意软件样本分析报告模板

> 使用范围：仅适用于经授权、在隔离实验环境中开展的样本分析。报告必须严格区分“已验证证据”“外部情报线索”和“待验证假设”；没有样本、原始输出或镜像时，不能填写为已执行。

## 1. 案例信息与授权

| 字段 | 内容 |
| --- | --- |
| 案例编号 | `<案例编号>` |
| 分析目的 | `<教学 / 检测验证 / 取证复核>` |
| 授权范围 | `<样本、系统、网络与时间范围>` |
| 分析人员与复核人 | `<姓名或角色>` |
| 实验环境 | `<Win11/Ubuntu 快照、内部网段、时区>` |
| 限制 | `<未获取样本、未执行动态分析、符号不匹配等>` |

## 2. 输入材料与完整性

| 材料 | 证据编号/路径 | SHA-256 | 状态 |
| --- | --- | --- | --- |
| 原始样本 | `<受控存储位置>` | `<SHA-256>` | `<只读原件 / 未提供>` |
| 工作副本 | `<受控存储位置>` | `<SHA-256>` | `<已创建 / 未创建>` |
| 内存镜像 | `<受控存储位置>` | `<SHA-256>` | `<已采集 / 未采集>` |
| 网络与服务端日志 | `<受控存储位置>` | `<SHA-256>` | `<已归档 / 未采集>` |

记录来源 URL、获取时间、保管责任人、文件大小、文件类型和访问限制。样本、PCAP、内存镜像、提取物和解密材料不得提交到 Git 仓库。

## 3. 工具、方法与原始输出

| 阶段 | 工具 | 目标 | 必留原始输出 | 执行状态 |
| --- | --- | --- | --- | --- |
| 完整性与静态初筛 | HashMyFiles、PEStudio、Detect It Easy、PE-bear、CFF Explorer | 哈希、文件类型、PE 结构、签名与节区 | 哈希清单、报告、截图、版本 | `<已执行 / 未执行>` |
| 字符串与能力线索 | FLARE-FLOSS、capa、LOKI + signature-base、HxD、CyberChef | 解码字符串、规则/能力线索、偏移 | 原始输出、规则版本、输入/输出哈希 | `<已执行 / 未执行>` |
| 运行时观察 | System Informer、Process Monitor、Noriben、Process Explorer、Autoruns、Regshot、API Monitor | 进程、模块、线程、文件/注册表/API 事件与启动项差异 | PML、CSV、截图、差异报告、工具版本 | `<已执行 / 未执行>` |
| 网络与服务端视图 | Wireshark、Fiddler、TCPLogView、FakeNet-NG，或 FakeDNS/Apache/INetSim/tcpdump | DNS、HTTP、会话、服务端日志与 PCAP | PCAP、服务日志、过滤器、五元组、时区 | `<已执行 / 未执行>` |
| 内存采集与解析 | FTK Imager/经验证的采集工具、Volatility 3、MemProcFS | 镜像、进程、VAD、模块、线程、网络对象 | 镜像哈希、采集日志、插件命令和完整输出 | `<已执行 / 未执行>` |

每行须补充工具版本、命令或 GUI 操作记录、执行 UTC、输入哈希及输出位置。详见[工具与自动化](../05-工具与自动化/README.md)和[内存取证标准分析工作流](../03-内存取证核心/05-标准分析工作流/README.md)。

## 4. 发现记录

| 编号 | 观察 | 证据来源 | 交叉验证 | 可信度 | 限制/替代解释 |
| --- | --- | --- | --- | --- | --- |
| F-01 | `<客观观察，不写推断>` | `<文件/日志/镜像输出位置>` | `<第二独立来源>` | `<高/中/低>` | `<缺失证据、时差、版本限制>` |

将“字符串命中”“规则命中”“在线沙箱标签”均视为线索。只有在文件、运行日志、PCAP、服务端记录或内存对象等独立来源相互支持时，才可形成行为结论。

## 5. 时间线与对象关联

| UTC 时间 | 主机 | 事件 | PID/对象 | 网络五元组/路径 | 证据编号 |
| --- | --- | --- | --- | --- | --- |
| `<YYYY-MM-DDThh:mm:ssZ>` | `<Win11/Ubuntu>` | `<事件>` | `<PID + 创建时间/对象地址>` | `<字段>` | `<证据编号>` |

优先使用 UTC；进程以 PID 加创建时间标识，地址须同时写明模块/VAD 上下文。先排除 PID 复用、ASLR、连接关闭和采集延迟，再解释差异。

## 6. 结论、置信度与后续动作

1. **结论**：`<仅陈述已由证据支持的事实>`。
2. **置信度**：`<高/中/低>`，依据为`<独立证据数及质量>`。
3. **未决问题**：`<缺失的样本、PCAP、镜像、符号或日志>`。
4. **后续动作**：`<仅限授权隔离环境中的验证步骤>`。

## 7. 附件清单

- [ ] 授权记录、来源记录和保管链。
- [ ] 原始样本/镜像的 SHA-256、文件大小和受控存储位置。
- [ ] 全部工具版本、命令/操作记录及未裁剪原始输出。
- [ ] PCAP、服务端日志、Procmon PML/CSV、截图与内存插件输出。
- [ ] 发现关联表、时间线与反证记录。
