# 20270612-004-b8ade8 样本分析报告

> 报告状态：已完成基于 PEStudio XML、capa、Noriben 和 INetSim 原始输出的静态与受控动态分析。本报告严格区分已观察到的行为、静态能力线索和未采集到的证据。
>
> 风险结论：**可疑，高风险处置建议**。样本在执行窗口内触发了对两个非常规 DNS 名称的查询，并同时具备加密、编码、PE 解析、运行时函数解析和反分析相关能力。由于未取得样本 PID 的完整文件/注册表/网络事件及 PCAP，本报告不将其直接定性为特定恶意软件家族或确认 C2 通信。

---

## 1. 案例信息

| 字段 | 内容 |
| --- | --- |
| 案例编号 | 20270612-004 |
| 样本文件名 | HOFUFX_Sign.exe |
| 样本标识 | b8ade8 |
| 分析时间 | 2026-07-12 |
| 分析环境 | Windows 主机 + Noriben 2.0.4 + INetSim + PEStudio Pro 9.57 + capa 9.40 |
| 动态执行时长 | 199.07 秒 |
| 样本路径记录 | %UserProfile%\Desktop\virusshare样本\20260712-004\HOFUFX_Sign.exe |
| 限制 | 未提供原始样本、PCAP、Procmon PML 或内存镜像；Noriben 未保留样本 PID 的文件/注册表事件。 |

## 2. 样本完整性与基础属性

| 项目 | 值 |
| --- | --- |
| MD5 | 551D6A9CACBB60C7D35D8AD4D0DAD283 |
| SHA-1 | 6D0F02AFCE3D870A39C2932AFC98106B0DBA159F |
| SHA-256 | B8ADE88891A97F4C05ACD714CA5330F920D322D6362D9EF24D0FCA5B478920CF |
| imphash | 913B837E2F4FE0BDE3F637B1C3F3E99B |
| 文件大小 | 1,243,424 Bytes |
| 文件类型 | Windows PE Executable |
| 架构 | AMD64 / 64-bit |
| 子系统 | GUI |
| 全文件熵 | 4.744 |
| 内嵌签名 | PEStudio 报告为 certificate 为空，未见嵌入式 Authenticode 签名。 |

HashMyFiles 原始输出未提供；本报告采用 capa 与 PEStudio XML 中一致的 MD5/SHA-256 记录。后续复核应以原始样本重新计算 SHA-256 为准。

---

## 3. 静态分析

### 3.1 PEStudio

PEStudio 记录的版本资源如下：

| 字段 | 值 |
| --- | --- |
| ProductName | PixlrCreativeInfinity Interpreter |
| ProductVersion | 1.4.3.6 |
| FileDescription | PixlrCreativeInfinity Interpreter Inc. |
| FileVersion | 9.6.4.0 |
| PDB 路径线索 | HOFUFX.pdb |
| Rich Header 工具链 | Visual Studio 2015 相关组件 |
| PE 链接器版本 | 14.40 |
| 编译/目录/调试时间戳 | 2026-01-09 09:22:24 |

版本资源、PDB 名称和文件名之间不存在直接的产品一致性证据；该差异应作为伪装或构建遗留线索记录，不能单独证明恶意性。PE 时间戳可被修改，不能作为可靠的编译时间结论。

文件包含 6 个常规节区：.text、.rdata、.data、.pdata、.rsrc 和 .reloc。可执行 .text 节熵为 6.381，未见异常高熵节区或明显的常见壳标记。PEStudio 同时记录了 2 个 TLS 回调和 3 个调试流；这些结构需要在调试或内存分析中进一步确认实际用途。

导入表只列出 msvcrt.dll、KERNEL32.dll 和 ntdll.dll。样本导入 CreateMutex、GetProcAddress、VirtualAlloc、VirtualProtect、VirtualQuery、IsDebuggerPresent、Sleep、NtWriteFile 和 TerminateProcess 等 API。capa 另识别到大量运行时函数解析能力，因此静态导入表中缺少网络库并不能排除运行时动态加载。

### 3.2 capa 能力分析

capa 以静态方式识别该文件为 Windows PE、AMD64，并识别 Rust 编译特征。以下结果均为**代码能力或规则命中**，不等同于已执行行为。

| 类别 | capa 观察 |
| --- | --- |
| 数据处理 | Base64 编码；AES 加密/解密；RC4 加密与伪随机序列生成。 |
| 文件与环境 | 查询环境变量、获取常用路径、获取文件属性、写入文件。 |
| 同步与进程 | 创建/打开互斥体；终止进程。 |
| 代码加载 | 解析 PE 头；解析 PE 导出以解析函数；在 Windows 上运行时链接函数。 |
| ATT&CK 映射 | T1140 解码/解密、T1027 混淆文件或信息、T1083 文件和目录发现、T1082 系统信息发现、T1129 共享模块。 |

PEStudio 对 Sleep、IsDebuggerPresent、VirtualAlloc、VirtualProtect 和 VirtualQuery 给出反沙箱、进程发现或进程注入相关映射。这些 API 在通用软件中亦可出现；本次动态材料未证明它们被用于反沙箱或进程注入，因此仅作为待验证线索。

### 3.3 静态评估

静态证据支持该样本具备以下特征：

- 使用 Rust 生态组件并包含 AES、RC4、Base64 相关实现。
- 可写文件、创建互斥体、解析 PE 结构并在运行时解析函数。
- 存在延时、调试器检查、TLS 回调和动态函数解析等需重点验证的反分析或加载线索。
- 不存在嵌入式 Authenticode 签名，且版本资源与 PDB/文件名之间的关系未获验证。

上述组合提高了可疑度，但仅凭静态结果不足以确认特定载荷、持久化、进程注入或数据窃取。

---

## 4. 动态分析

### 4.1 进程与 Noriben 结果

Noriben 2.0.4 记录到 Explorer.EXE（PID 5044）启动样本：

~~~text
HOFUFX_Sign.exe
Child PID: 6136
~~~

Noriben 报告的运行时长为 199.07 秒。除创建进程记录外，报告中未出现 PID 6136 或 HOFUFX_Sign.exe 归属的文件、注册表或网络事件。

报告中的文件活动由 svchost.exe 写入 Windows 硬件错误事件服务（Whesvc）目录；报告中的注册表活动由 svchost.exe、ctfmon.exe 和 Explorer.EXE 产生。它们不能归因于样本。网络摘要也只显示 svchost.exe 与隔离 DNS 服务之间的 UDP 流量。

因此，本次动态分析的正确结论是：

- 已验证样本由 Explorer.EXE 启动，PID 为 6136。
- 未从提供的 Noriben 输出中捕获样本 PID 的文件、注册表、子进程或网络事件。
- 这不表示样本未执行这些行为；可能与 Procmon 过滤、事件丢失、Noriben 配置或仅保留摘要有关。

### 4.2 INetSim 网络结果

INetSim 在 2026-07-12 13:44:16 至 13:44:30 记录到主机 101.101.101.128 的 DNS 查询。以下项目属于系统或网络环境噪声，不能归因于样本：

| 时间 | 查询 | 说明 |
| --- | --- | --- |
| 13:44:16 | activity.windows.com | Windows 遥测相关名称。 |
| 13:44:17、13:44:30 | 多个 PTR 查询 | INetSim 地址、IPv4/IPv6 反向解析相关查询。 |

同一执行窗口内还出现以下两个 A 记录查询：

~~~text
98ba5f6.dns1.dreamls.com
98ba5f6.dns2.dreamls.com
~~~

两次查询相隔约 5 秒，名称使用相同的 7 位十六进制前缀，并分别访问 dns1、dns2 子域。INetSim 将其应答为 127.0.0.1，提供的日志未显示后续 TCP、HTTP 或 HTTPS 会话。

**网络判断：**

- 这两个 dreamls.com 名称是本报告最重要的网络 IOC 和可疑行为线索。
- 其查询时间与样本执行窗口一致，但 INetSim 日志只记录主机 IP，Noriben 网络摘要只归因到 DNS 客户端 svchost.exe；没有 PCAP 或按 PID 的网络遥测，故不能以高置信度确认该查询由 PID 6136 发起。
- 未观察到成功的载荷下载、HTTP/HTTPS 请求、IP 直连或已建立的远程控制会话。

---

## 5. IOC 与检测线索

### 文件 IOC

~~~text
文件名: HOFUFX_Sign.exe
MD5: 551D6A9CACBB60C7D35D8AD4D0DAD283
SHA-1: 6D0F02AFCE3D870A39C2932AFC98106B0DBA159F
SHA-256: B8ADE88891A97F4C05ACD714CA5330F920D322D6362D9EF24D0FCA5B478920CF
imphash: 913B837E2F4FE0BDE3F637B1C3F3E99B
PDB: HOFUFX.pdb
~~~

### 网络 IOC

~~~text
98ba5f6.dns1.dreamls.com
98ba5f6.dns2.dreamls.com
~~~

以上域名为运行窗口内的可疑 DNS 线索，尚未由样本 PID 或 PCAP 独立归因；检测策略应先以观察和告警为主，并结合端点进程、DNS 客户端事件和网络会话复核。

---

## 6. 风险评估

| 项目 | 结论 | 证据强度 |
| --- | --- | --- |
| 可疑 DNS 查询 | 在样本执行窗口内观察到两个 dreamls.com 子域查询。 | 中 |
| 加密/编码能力 | capa 识别 AES、RC4 和 Base64 相关能力。 | 中 |
| 动态函数解析与 PE 解析 | capa 识别运行时链接、解析 PE 头及导出函数。 | 中 |
| 反分析线索 | Sleep、IsDebuggerPresent、TLS 回调等静态线索。 | 低至中 |
| 文件写入能力 | capa 命中 Windows 文件写入与 NtWriteFile。 | 中 |
| 持久化 | 未发现可归因于样本的启动项、服务、计划任务或注册表持久化。 | 低；动态捕获不完整 |
| 进程注入 | 仅存在 VirtualAlloc/VirtualProtect/VirtualQuery 静态线索；未观察到注入行为。 | 低 |
| 载荷下载/C2 | 未观察到后续 HTTP/HTTPS 或 TCP 会话；DNS 查询无法按 PID 归因。 | 未证实 |

**综合评级：高风险处置建议。** 评级依据是可疑 DNS 查询与多项加载、加密、反分析能力的组合，而不是对某一单项证据的过度解读。建议隔离该文件和出现上述域名查询的主机，保留日志，并在补充网络和端点证据前避免将其归类为确定的某一恶意软件家族。

---

## 7. 后续验证建议

1. 重新运行动态分析，Procmon 过滤条件应包含 PID 6136 及全部后代进程，并保留原始 PML。
2. 同步采集 PCAP、Windows DNS Client Operational 日志和 Sysmon 网络事件，以 PID、时间和查询名称交叉归因 dreamls.com 查询。
3. 在 DNS 返回可控地址后，记录后续 TCP/HTTP/HTTPS 连接、请求路径、SNI、User-Agent 和响应内容。
4. 对进程创建时和 DNS 查询后采集内存镜像，检查模块、VAD、互斥体、解密缓冲区、线程起始地址和网络对象。
5. 复核样本的数字签名、版本资源来源和 HOFUFX.pdb 引用；对 Rust 字符串、加密常量和 DNS 前缀生成逻辑进行逆向分析。
6. 在同一隔离环境中以多次快照复测，确认 DNS 名称是否固定、是否随时间变化，以及是否与主机背景流量无关。

---

## 8. 证据来源

| 证据 | 用途 |
| --- | --- |
| 20270612-004-b8ade8分析报告.xml | PEStudio 静态信息、节区、版本资源、导入、PDB、TLS、签名状态和 API 线索。 |
| capa.txt | 文件哈希、架构、Rust、加密、文件、互斥体、运行时链接和 ATT&CK 能力。 |
| Noriben_12_Jul_26__13_44_940747.txt | 样本创建进程、执行时长、未捕获样本 PID 后续事件的限制。 |
| inetsim.txt | DNS 查询时间、主机地址、dreamls.com 子域及模拟应答。 |
